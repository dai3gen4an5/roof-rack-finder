/**
 * Canonical site URL used for metadata, canonical links, and sitemap/robots
 * generation. Set NEXT_PUBLIC_SITE_URL before deploying — this fallback is a
 * placeholder, not a real domain.
 */
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? "https://rackfit.example";

export const SITE_NAME = "RackFit";
